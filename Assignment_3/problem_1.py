## Write a program that takes a string as input and returns its length without using `len()`.

string=input("Enter string: ")
count=0
for i in string:
      count=count+1
print("Length of the string is:",count)

